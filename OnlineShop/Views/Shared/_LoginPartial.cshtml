@using Microsoft.AspNetCore.Identity
@inject SignInManager<Model.Models.ApplicationUser> SignInManager
@inject UserManager<Model.Models.ApplicationUser> UserManager



@if (SignInManager.IsSignedIn(User))
{

    <a class="nav-link" href="#" title="Manage">
        Cuenta
    </a>
}
else
{
    <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Cuenta</a>
}

<ul class="dropdown-menu">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item">
            <a class="nav-link" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Perfil</a>
        </li>


        <li class="nav-item">
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button type="submit" class="nav-link btn btn-link btn-block text-sm-left">Salir</button>
            </form>
        </li>
    }
    else
    {

        <li class="nav-item"><a class="nav-link" asp-area="Identity" asp-page="/Account/Login">Login</a></li>
        <li class="nav-item"><a class="nav-link" asp-area="Identity" asp-page="/Account/Register">Registro</a></li>
    }
</ul>